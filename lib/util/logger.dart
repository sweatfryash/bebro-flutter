import 'package:logger/logger.dart';

class Log {
  /*static Logger logger = Logger(printer: PrettyPrinter());

  get i => logger.i;
  get d => logger.d;
  get e => logger.e;
  get w => logger.w;*/
}
